<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- AppCompatEditText默认状态状态设置底线颜色 -->
<!--        <item name="colorControlNormal">#xxxx</item>-->
        <!-- AppCompatEditText选择的底线颜色 -->
        <item name="colorControlActivated">@color/blue</item>
    </style>

    <!-- 应用启动页(StartingWindow)的theme -->
    <style name="StartingWindowTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 可以设置成纯颜色（设置一个和Activity UI相似的背景） -->
        <!--<item name="android:windowBackground">@color/startingwindow_bgcolor</item>-->
        <!--也可以设置成一张图片 -->
        <!--<item name="android:windowBackground">@mipmap/boot_start</item>-->
<!--        <item name="android:background">@mipmap/boot_start</item>-->
        <!--一定要家，状态栏显示-->
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
