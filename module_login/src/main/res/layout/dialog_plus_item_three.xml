<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#fff"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:padding="5dp"
        android:id="@+id/dialog_item_name"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:gravity="center_vertical"
        android:text="名都苑施工地段"
        android:textColor="@color/colorPrimary"
        android:textSize="14sp"
        tools:ignore="MissingConstraints" />

    <TextView
        android:text="状态"
        app:layout_constraintBottom_toBottomOf="@+id/dialog_item_name"
        app:layout_constraintTop_toTopOf="@+id/dialog_item_name"
        android:id="@+id/dialog_item_true"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:gravity="center"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginEnd="30dp"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        android:layout_marginRight="30dp" />
    <TextView
        android:id="@+id/dialog_item_line"
        android:layout_margin="5dp"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        app:layout_constraintTop_toBottomOf="@+id/dialog_item_name"
        android:background="#eee"/>

</androidx.constraintlayout.widget.ConstraintLayout>